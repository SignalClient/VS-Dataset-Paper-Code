# ECG 重建建议

不同人的原始相位略有区别，导致二阶差分后的图形不完全一致，这是正常现象。

原因包括：

- 胸壁运动幅度不同
- 呼吸频率和呼吸谐波不同
- 心脏机械振动方向不同
- 雷达照射角度不同
- 相位极性可能相反
- 个体体型、距离、姿态不同

所以如果要做 **ECG 重建**，不要只依赖某一种固定的二阶差分波形。

## 1. 先明确 ECG 重建目标

毫米波雷达测到的是胸壁机械运动，而 ECG 是心脏电活动信号。二者相关，但不是同一个物理量。

因此建议优先做：

- R 峰时间重建
- RR interval 重建
- 心率 / 心律节律重建
- ECG-like 波形生成

不要一开始就强行要求完整还原 P 波、QRS 波、T 波。

## 2. 不要只用二阶差分作为输入

建议把雷达信号处理成多通道特征，例如：

```text
raw_phase
respiration_removed_phase
heartbeat_phase
second_diff_after_remove_resp
second_diff_raw
bandpass_0.7_20
```

也就是说，模型输入不应该只有：

```text
二阶差分结果
```

而应该是：

```text
多种雷达相位特征组合
```

这样模型可以自己学习不同人身上哪种特征更有用。

## 3. 每个人都要做窗口级归一化

不同人的幅值差异很大，不能直接喂给模型。

建议每个 5-10 秒窗口做：

- 去中位数
- 除以 IQR 或标准差
- 限幅到固定范围，例如 `[-5, 5]`
- 必要时做极性校正

目标是让模型关注：

```text
波形结构 + 峰值时间 + 节律变化
```

而不是关注绝对幅值。

## 4. 优先做 R 峰监督

训练时不要只用 ECG 波形 MSE。

更稳的方法是先从 ECG 中提取 R 峰，然后把 R 峰做成高斯标签：

```text
ECG R peak -> Gaussian pulse target
```

模型学习输出 R 峰概率序列。

这样雷达信号只要能对齐 ECG 的 R 峰位置，就可以先把节律学出来。

## 5. 推荐两阶段 ECG 重建

更稳定的流程是：

```text
第一阶段：
雷达信号 -> R 峰 / RR interval

第二阶段：
R 峰序列 + 雷达局部特征 -> ECG-like waveform
```

这样比直接做：

```text
雷达信号 -> ECG 波形
```

更稳。

## 6. 推荐模型

数据量不是特别大的情况下，建议先用：

- 1D U-Net
- TCN
- 小型 CNN-BiLSTM

不要一开始就用很大的 Transformer。

比较推荐：

```text
输入：多通道雷达特征
输出：ECG R 峰概率序列
```

后面再扩展到：

```text
输出：ECG waveform
```

## 7. 评估方式必须按人划分

不能随机切窗口训练测试，否则同一个人的信号会同时出现在训练集和测试集里，结果会虚高。

推荐：

```text
Leave-one-subject-out
```

例如：

```text
VS01-VS23 训练
VS24 测试
```

这样才能判断模型是否真的能泛化到新人。

## 总结

二阶差分结果可以作为 ECG 重建的重要特征，但不能作为唯一特征。

更合理的 ECG 重建路线是：

```text
自适应去呼吸
+ 多通道雷达相位特征
+ 窗口级归一化
+ R 峰监督
+ 按人划分验证
```

这样才能减少不同人原始相位差异带来的影响。
