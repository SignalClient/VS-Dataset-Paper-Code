# VS_DATASET 代码流程分析与运行说明

## 1. 这个仓库在做什么

这个仓库提供了一个基于 120 GHz 毫米波雷达的生命体征数据处理示例，主要目标是：

- 从雷达测量中提取生命体征信号，例如呼吸和心跳；
- 与参考信号（如 Mindray ECG/呼吸参考）做对比和同步验证；
- 生成可视化图像，辅助结果分析。

对应的核心脚本包括：

- [Test_speaker.m](Test_speaker.m)：雷达微位移验证流程，演示从原始 beat tone 到位移相位信号的完整处理。
- [VS_separation.m](VS_separation.m)：对数据集中的雷达 vital 信号进行呼吸/心跳分离。
- [TechValidation.m](TechValidation.m)：做雷达信号与参考信号的同步验证和统计分析。
- [PlotVS.m](PlotVS.m)：可视化雷达与参考信号波形。
- [plotPhasewu.m](plotPhasewu.m)：辅助绘制相位/位移变化图。

---

## 2. 数据组织方式

仓库假设数据目录结构如下：

```text
VITALSENSE_120_DATASET/
  VS01/
    VS01_Resting.mat
    VS01_Resting_Mindray.mat
    VS01_Apnea.mat
    VS01_Apnea_Mindray.mat
  ...
  VS24/
    VS24_Resting.mat
    VS24_Resting_Mindray.mat
    VS24_Apnea.mat
    VS24_Apnea_Mindray.mat
```

其中：

- 以 “_Resting.mat” 或 “_Apnea.mat” 结尾的是雷达结果文件；
- 以 “_Mindray.mat” 结尾的是参考信号文件；
- 每个受试者包含 2 种场景：静息和 Apnea。

---

## 3. 代码处理流程总览

### 3.1 典型流程

1. 读取雷达数据和参考数据；
2. 对雷达 vital 信号做滤波和分离；
3. 提取呼吸/心跳成分；
4. 与参考信号做同步验证；
5. 生成图像和统计结果。

### 3.2 代码对应关系

| 脚本 | 作用 | 典型输出 |
|---|---|---|
| [Test_speaker.m](Test_speaker.m) | 处理原始雷达采样二进制数据，提取微位移信号 | 时域/频域图、位移信号图 |
| [VS_separation.m](VS_separation.m) | 对已存储的雷达 vital 信号做呼吸和心跳分离 | 分离后的呼吸/心跳图 |
| [TechValidation.m](TechValidation.m) | 计算雷达和参考信号之间的时间延迟和相关性 | 统计结果、图表、CSV/MAT 文件 |
| [PlotVS.m](PlotVS.m) | 可视化多通道参考信号与雷达波形 | 对比图 |
| [plotPhasewu.m](plotPhasewu.m) | 计算并绘制相位/位移曲线 | 相位图 |

---

## 4. 每个脚本的详细分析

### 4.1 [Test_speaker.m](Test_speaker.m) 的流程

这是一个“从原始二进制雷达采样数据到位移信号”的完整处理链路。

#### 4.1.1 读取原始数据

脚本先通过 `uigetfile` 弹出文件选择窗口，让用户选择一个 `.bin` 文件。随后：

- 读取文件头信息：`chans`、`long`、`recs`、`buffs`；
- 再读取实际的数据块；
- 将数据重排成 `Ch` 矩阵；
- 去掉直流偏置，得到 beat tone 时间序列。

这一步相当于把原始采样数据从二进制格式转换为 MATLAB 中可分析的矩阵。

#### 4.1.2 FFT 频谱分析

接下来对每个 chirp 做加窗和 FFT：

- 使用 Hann 窗减少频谱泄漏；
- 做零填充（zero-padding）提高频谱分辨率；
- 得到每个时间快拍对应的频谱；
- 通过峰值检测寻找目标频率分量。

#### 4.1.3 相位提取与位移计算

脚本会在频谱中找出峰值对应的频率点，然后利用相位变化计算位移：

- 对频域中的目标频率点取相位；
- 进行相位解包（unwrap）；
- 根据雷达波长、频率和几何关系换算成位移；
- 最终得到微位移信号，也就是微小身体运动对应的位移曲线。

#### 4.1.4 输出内容

脚本会生成：

- 时域和频域图；
- 相位/位移变化图；
- 最终的微位移信号图。

#### 4.1.5 这个脚本的核心意义

它展示的是“原始雷达回波 → 频谱峰值 → 相位 → 位移”的完整过程，适合验证雷达微运动感知能力。

---

### 4.2 [VS_separation.m](VS_separation.m) 的流程

这是针对数据集的主处理脚本，目标是从雷达 vital 信号中分离呼吸和心跳。

#### 4.2.1 加载数据

脚本会从数据集路径中加载：

- 雷达文件：例如 `VS05_Resting.mat`；
- 参考文件：例如 `VS05_Resting_Mindray.mat`。

#### 4.2.2 读取关键变量

从 `.mat` 文件中读取：

- `VitalSig`：雷达提取到的 vital 信号；
- `T_frame`：采样时间间隔；
- `ECGSig`：参考 ECG；
- `Fs_ecg`：ECG 采样率；
- `respiration`：参考呼吸信号（在某些文件中会用到）。

#### 4.2.3 滤波分离

这里使用 FIR 低通滤波：

- 先用低通滤波得到呼吸主导信号；
- 然后用原始信号减去该低频分量，得到心跳相关信号。

这一步的思路是：

- 呼吸频率通常较低；
- 心跳信号是较高频的微小波动；
- 通过滤波把它们分离开。

#### 4.2.4 绘图输出

脚本会生成两个图：

1. 呼吸信号提取图；
2. 心跳信号与 ECG 对比图。

---

### 4.3 [TechValidation.m](TechValidation.m) 的流程

这个脚本的目标是做“技术验证”，重点检查雷达信号与参考信号的同步性和相关性。

#### 4.3.1 循环处理所有受试者和场景

脚本会遍历：

- 24 个受试者；
- 2 个场景：`Resting` 和 `Apnea`。

#### 4.3.2 数据预处理

对每一条记录：

- 加载雷达和参考文件；
- 读取呼吸参考信号 `respiration`；
- 读取雷达 vital 信号；
- 把两个信号重采样到 2000 Hz。

#### 4.3.3 互相关分析

然后使用 `xcorr` 做互相关：

- 计算雷达信号和参考呼吸信号之间的相关性；
- 找到相关系数最大的时延；
- 认为这就是两个信号的时间滞后。

#### 4.3.4 输出结果

脚本会：

- 把每个样本的滞后时间和相关性存入 `results` 结构体；
- 生成统计摘要；
- 绘制箱线图和散点图；
- 保存为：
  - `Technical_validation_results.mat`
  - `Technical_validation_results.csv`

#### 4.3.5 这个脚本的用途

它可以用来验证：

- 雷达测量与参考系统是否同步；
- 不同受试者/不同场景下的时延分布如何；
- 相关性质量是否足够好。

---

### 4.4 [PlotVS.m](PlotVS.m) 的流程

这个脚本主要用于绘图展示。

#### 4.4.1 加载数据

脚本会加载指定受试者的雷达和参考信号数据。

#### 4.4.2 归一化与偏移

由于多个信号幅值差异很大，因此脚本会：

- 对各个信号做缩放；
- 加上不同的偏移量；
- 叠加到同一张图上，方便视觉比较。

#### 4.4.3 输出内容

它会画出：

- 雷达 vital 信号；
- 呼吸参考信号；
- 脉搏/血氧参考信号；
- ECG 信号。

这个脚本更偏向“展示效果”，而不是算法核心处理。

---

### 4.5 [plotPhasewu.m](plotPhasewu.m) 的作用

这个函数是 [Test_speaker.m](Test_speaker.m) 的辅助函数，负责绘制相位图：

- 取目标频率点的相位；
- 可选择是否做相位解包；
- 绘制相位随时间变化的曲线；
- 同时在右轴叠加幅度变化。

它把相位信息转成可观察的图，帮助判断微位移信号质量。

---

## 5. 运行方式

### 5.1 环境要求

- MATLAB（推荐 R2024b 或更高版本，仓库说明中提到）
- Windows 系统（仓库作者测试环境）
- 需要准备好数据集文件

### 5.2 推荐步骤

1. 打开 MATLAB；
2. 将当前工作目录切到仓库根目录；
3. 确保数据目录路径正确；
4. 根据需要运行对应脚本。

### 5.3 运行 [VS_separation.m](VS_separation.m)

步骤：

1. 打开 [VS_separation.m](VS_separation.m)；
2. 把其中的 `dataPath` 改成你本地数据集根目录；
3. 选择一个受试者，例如 `VS05`；
4. 选择场景，例如 `Resting`；
5. 点击运行。

你需要修改的内容大致是：

```matlab
dataPath='...YOUR PATH TO DATASET FOLDER...';
subjID='VS05';
scenario='Resting';
```

### 5.4 运行 [TechValidation.m](TechValidation.m)

步骤：

1. 打开 [TechValidation.m](TechValidation.m)；
2. 把 `dataPath` 改成实际数据集路径；
3. 运行脚本。

脚本会自动：

- 处理 24 名受试者；
- 生成统计结果；
- 输出图像和文件。

### 5.5 运行 [PlotVS.m](PlotVS.m)

步骤：

1. 打开 [PlotVS.m](PlotVS.m)；
2. 现在脚本会自动从当前仓库目录下的 `VITALSENSE_120_DATASET` 查找数据；
3. 默认使用 `VS05` 的 `Resting` 场景；
4. 运行脚本即可。

如果你想换成别的受试者或场景，可以修改脚本中的：

```matlab
subjID = 'VS05';
scenario = 'Resting';
```

### 5.6 运行 [Test_speaker.m](Test_speaker.m)

步骤：

1. 打开 [Test_speaker.m](Test_speaker.m)；
2. 确保你有一个 `.bin` 原始雷达采样文件；
3. 运行脚本；
4. 在弹出的文件选择窗口中选择对应的 `.bin` 文件。

注意：

- 这个脚本依赖原始二进制采样文件；
- 如果你没有对应的 `.bin` 文件，脚本无法完整运行；
- 运行时会弹出文件选择框，而不是直接指定路径。

---

## 6. 常见注意事项

- 代码中多处使用了占位路径，例如 `...YOUR PATH TO DATASET FOLDER...`，必须替换成你的真实路径；
- [Test_speaker.m](Test_speaker.m) 需要你手动选择 `.bin` 文件；
- 数据文件名必须与脚本中定义的文件名一致；
- 如果你使用的 MATLAB 版本不同，某些函数的默认行为可能略有差异；
- 结果图会在 MATLAB 画布中显示，同时部分脚本会保存结果文件。

---

## 7. 推荐的实际使用顺序

如果你是第一次运行这个项目，建议按下面顺序：

1. 先运行 [VS_separation.m](VS_separation.m)，确认能正确读取并分离信号；
2. 再运行 [TechValidation.m](TechValidation.m)，得到同步验证结果；
3. 最后根据需要运行 [PlotVS.m](PlotVS.m) 和 [Test_speaker.m](Test_speaker.m)。

---

## 8. 一句话总结

这个仓库的核心是：

- 用毫米波雷达测量生命体征；
- 通过信号处理把呼吸和心跳从雷达数据中分离出来；
- 再和参考信号做同步验证和可视化展示。
